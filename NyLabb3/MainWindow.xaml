<Window x:Class="NyLabb3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quiz App" Height="450" Width="800">
    <Grid>
        
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="LightBlue" Offset="0.0" />
                <GradientStop Color="DodgerBlue" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            
            <Button Content="Start Quiz" FontSize="24" Padding="20,10" 
                    Click="StartQuiz_Click" Margin="0,0,0,10"/>
            <Button Content="Create New Quiz" FontSize="24" Padding="20,10" 
        Click="CreateQuiz_Click"  Margin="0,0,0,10"/>
            <Button Content="Exit" FontSize="24" Padding="20,10" 
        Click="Exit_Click" Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</Window>